import { AppFilters } from 'features/PacksList/models/FiltersModel'

export const setUrlParams = (param: AppFilters, payload: string) => {
  /* const currentSearchParams = [...searchParams.entries()].reduce(
        (acc, [param, value]) => ({
          ...acc,
          [param]: value
        }),
        {}
      )
    
      const newSearchParams = { ...currentSearchParams, [param]: payload }
    
      if (action === 'set') {
        console.log(newSearchParams)
      } */
}
